import { DesignSystemPage } from '@/components/ui/design-system'
import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Design System — TableVista' }

export default function Page() {
  return <DesignSystemPage />
}
